import java.sql.*;

public class WDB{
 public static void main(String args[]) throws Exception{
  //drive loading
  Class.forName("oracle.jdbc.driver.OracleDriver");
  
  String url =  "jdbc:oracle:thin:@172.20.2.139:1521:orcl";
  String id = "hrm";
  String pw = "hrm";
  //connect Database
  Connection conn = DriverManager.getConnection(url,id,pw);
  if(conn != null){
   System.out.println("데이터베이스와 연결 성공");
  }
  
  //데이터베이스로 보낼 쿼리문 생성
  Statement stmt = conn.createStatement();
  
  //결과레코드들을 저장할 집합 변수를 만듬.
  ResultSet rs;
  //질의 보내고 결과를 받는다.
  rs = stmt.executeQuery("select * from y_emp");
  
  
  //첫번째 레코드로 커서를 이동시키는 영역
  rs.next();
  //첫번째 레코드만 출력합시다.
  //이름을 얻어온다.
//  String name = rs.getString("emp_name");
//  System.out.println(name);
  
//  stmt.close();
  
  while(rs.next()){
   String name = rs.getString("emp_name");
   System.out.print(name+" ,");
   
   String job = rs.getString("position");
   System.out.print(job+", ");
   
   String gender = rs.getString("gender");
   System.out.print(gender+" ,");
   
   Date birthdate = rs.getDate("birthdate");
   System.out.print(birthdate+" ");
   System.out.println();
  }
  
  if(conn != null){
   conn.close();
  }
 }
}
[출처] 영진전문대학 컴퓨터정보계열 1301311 황재웅|작성자 hhj5828

